# Spike Workload Scenario
# Rapid surge of concurrent requests to evaluate HPA trigger response time and queue buffering
locustfile = load-testing/locustfile.py
headless = true
host = http://localhost:30080
users = 60
spawn-rate = 20
run-time = 45s
